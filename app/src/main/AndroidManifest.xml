<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ru.kurepin.bookdepository">

    <!-- ==================== РАЗРЕШЕНИЯ ==================== -->
    <!-- Разрешение на использование камеры -->
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- Разрешение на запись во внешнее хранилище (для сохранения фото) -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />

    <!-- Разрешение на чтение внешнего хранилища -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <!-- =================================================== -->

    <!-- ==================== ОБЪЯВЛЕНИЕ ИСПОЛЬЗОВАНИЯ КАМЕРЫ ==================== -->
    <!-- required="false" означает, что приложение может работать без камеры -->
    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />
    <!-- ======================================================================== -->

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BookDepository">

        <!-- Главная активность (список книг) -->
        <activity
            android:name=".BookListActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Активность для пролистывания книг -->
        <activity
            android:name=".BookPagerActivity"
            android:label="@string/app_name"
            android:parentActivityName=".BookListActivity" />

        <!-- Активность для редактирования книги -->
        <activity
            android:name=".BookActivity"
            android:parentActivityName=".BookListActivity" />

        <!-- FileProvider для работы с камерой на Android 7.0+ -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/provider_paths" />
        </provider>

    </application>

</manifest>